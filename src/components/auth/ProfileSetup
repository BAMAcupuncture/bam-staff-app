import React from 'react';

const ProfileSetup: React.FC = () => {
  // You can get user info from context if needed in the future
  // For now, it's a static message.
  return (
    <div className="flex justify-center items-center h-screen">
        <div className="text-center p-8 bg-white shadow-md rounded-lg">
            <h2 className="text-2xl font-bold text-gray-800">Profile Setup Required</h2>
            <p className="mt-2 text-gray-600">
                Your login account exists, but your user profile has not been created in the database.
            </p>
            <p className="mt-4 text-sm text-gray-500">
                Please contact an administrator to have them add you to the team.
            </p>
        </div>
    </div>
  );
};

export default ProfileSetup;